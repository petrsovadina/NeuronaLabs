{
  "name": "neuronalabs",
  "version": "1.0.0",
  "scripts": {
    "start": "docker-compose up",
    "build": "docker-compose build",
    "init-supabase": "node scripts/initialize-supabase.js",
    "migrate": "ts-node scripts/supabase-migrate.ts",
    "create-migration": "ts-node scripts/create-migration.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "dotenv": "^16.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
